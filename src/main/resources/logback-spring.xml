<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 로컬용 -->
    <springProfile name="local">
        <include resource="logback-local.xml"/>
    </springProfile>

    <!-- 도커용 -->
    <springProfile name="docker">
        <include resource="logback-docker.xml"/>
    </springProfile>
</configuration>